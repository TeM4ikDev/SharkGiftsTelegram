export const paymentConfig = {
  // Целевой адрес кошелька для получения платежей
  // Установите здесь реальный адрес TON кошелька
  targetWalletAddress: process.env.TARGET_WALLET_ADDRESS,
  
  // Минимальная сумма платежа в TON
  minAmount: 0.1,
  
  // Максимальная сумма платежа в TON
  maxAmount: 1000,
  
  // Время жизни платежа в секундах (30 минут)
  paymentLifetime: 1800,
  
  // Интервал проверки транзакций в миллисекундах (10 секунд)
  checkInterval: 10000,
  
  // Максимальное количество попыток проверки
  maxCheckAttempts: 180, // 30 минут при интервале 10 секунд
  
  // Количество транзакций для проверки
  transactionsToCheck: 100,
  
  // Время ожидания между повторными запросами к API (мс)
  apiRetryDelay: 2000,
};
